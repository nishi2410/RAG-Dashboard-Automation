name: Docker Build on PROD

on:
  push:
    branches:
      - PROD

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build Docker image (no cache)
        run: docker build --no-cache -t fastapi-app .
